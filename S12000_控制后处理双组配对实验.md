# s12000 控制后处理双组配对实验

> **运行位置：仅限实验室评测主机。** 当前笔记本性能不足，不得在笔记本上运行
> smoke、pilot 或正式 rollout。笔记本 `outputs/eval/` 中带
> `LOCAL_INVALID_DO_NOT_USE.txt` 的目录只用于早期链路检查，禁止复制、续跑或纳入统计。

## 1. 目标与边界

实验比较同一个 s12000 checkpoint 在两种控制栈下的 MuJoCo 闭环效果：

| 组别 | Chunk blend | Motion limiter | Rollout |
| --- | ---: | --- | ---: |
| A：原始控制基线 | K0 | 关闭 | 120 |
| B：联合控制组 | K4 | `mug_v1_p99x1.1.json` | 120 |

Checkpoint 固定为：

```text
outputs/train/smolvla_ur10e_mug_v1_b8_s12000/checkpoints/012000/pretrained_model
```

`model.safetensors` SHA-256 为
`d363f2c7911ea95f5c6d5885915ab5bebec5330718a99c627adc6fd40480ed76`。
结果只代表 s12000 在两种控制栈下的仿真表现；B 组是 K4 与 limiter 的联合效果，
不能拆分两项功能的独立贡献，也不与 s8000 做统计推断。

## 2. 锁定矩阵

两组使用完全相同的 20 个未见 scene、2 个任务、canonical prompt 和 policy seed
`20260/20261/20262`，即每组 `20 × 2 × 1 × 3 = 120` 条。固定
`fps=20`、`max_steps=360`、`execution_horizon=25` 和原始杯子外观。

- A 配置：`configs/eval/mug_v1_s12000_unseen_multiseed_baseline.yaml`
- B 配置：`configs/eval/mug_v1_s12000_unseen_multiseed_k4_limiter.yaml`

Blend 是命令行参数。首次运行和续跑都必须显式保留对应的
`--chunk-blend 0/4`，否则 manifest 身份校验应拒绝续跑。

## 3. 主机准备与验签

把代码、两份配置、配对分析脚本和完整 `012000/pretrained_model` 放到实验室主机的
同一个项目目录。不要上传或复制笔记本的 `outputs/eval/`。进入主机项目根目录后执行：

```powershell
conda activate smolvla-eval

# 必须确认当前解释器、CUDA和主机GPU正确。
python -c "import sys, torch, mujoco, lerobot; print(sys.executable); print(torch.__version__, torch.cuda.is_available(), torch.cuda.get_device_name(0)); print(mujoco.__version__, lerobot.__version__)"

# 必须与本文记录的s12000哈希完全一致。
Get-FileHash `
  outputs\train\smolvla_ur10e_mug_v1_b8_s12000\checkpoints\012000\pretrained_model\model.safetensors `
  -Algorithm SHA256

# 验证两组矩阵和分析工具，不推进MuJoCo正式实验。
python -m unittest `
  tests.test_evaluate.ControlStackComparisonTests `
  tests.test_evaluate.EvaluationContractTests.test_s12000_control_stack_configs_share_the_same_120_rollouts `
  tests.test_chunk_blend -v
```

预期版本为 PyTorch `2.7.0+cu126`、MuJoCo `3.6.0`、LeRobot `0.4.4`，且
`torch.cuda.is_available()` 必须为 `True`。首次正式运行前确认下列主机输出目录不存在；
如果目录来自本实验的中断运行，则保留并使用 `--resume`，不要删除后重跑：

```powershell
Test-Path outputs\eval\s12000_unseen_multiseed_baseline
Test-Path outputs\eval\s12000_unseen_multiseed_k4_limiter
```

## 4. Smoke 与 Pilot

在实验室主机上为两组使用互不复用的输出目录：

```powershell
# A组：加载、CUDA、场景和产物链路
.\evaluate\run.ps1 `
  --checkpoint outputs\train\smolvla_ur10e_mug_v1_b8_s12000\checkpoints\012000\pretrained_model `
  --config configs\eval\mug_v1_s12000_unseen_multiseed_baseline.yaml `
  --output-dir outputs\eval\s12000_control_stack_smoke_baseline `
  --chunk-blend 0 --max-rollouts 1 --max-steps 2

# B组：同时验证K4和limiter接入
.\evaluate\run.ps1 `
  --checkpoint outputs\train\smolvla_ur10e_mug_v1_b8_s12000\checkpoints\012000\pretrained_model `
  --config configs\eval\mug_v1_s12000_unseen_multiseed_k4_limiter.yaml `
  --output-dir outputs\eval\s12000_control_stack_smoke_k4_limiter `
  --chunk-blend 4 --max-rollouts 1 --max-steps 2
```

Smoke 通过后运行两组 pilot。这六条覆盖首个 scene 的两个任务和三个 policy seed：

```powershell
# A组pilot
.\evaluate\run.ps1 `
  --checkpoint outputs\train\smolvla_ur10e_mug_v1_b8_s12000\checkpoints\012000\pretrained_model `
  --config configs\eval\mug_v1_s12000_unseen_multiseed_baseline.yaml `
  --output-dir outputs\eval\s12000_control_stack_pilot_baseline `
  --chunk-blend 0 --max-rollouts 6

# B组pilot
.\evaluate\run.ps1 `
  --checkpoint outputs\train\smolvla_ur10e_mug_v1_b8_s12000\checkpoints\012000\pretrained_model `
  --config configs\eval\mug_v1_s12000_unseen_multiseed_k4_limiter.yaml `
  --output-dir outputs\eval\s12000_control_stack_pilot_k4_limiter `
  --chunk-blend 4 --max-rollouts 6
```

Smoke 和 pilot 的通过条件是进程退出码为 0、无 `control_exception`、manifest 中
A 组为 K0/limiter false、B 组为 K4/limiter true。Pilot 成功率不进入正式统计。

## 5. 正式运行、监控与恢复

```powershell
# A组
.\evaluate\run.ps1 `
  --checkpoint outputs\train\smolvla_ur10e_mug_v1_b8_s12000\checkpoints\012000\pretrained_model `
  --config configs\eval\mug_v1_s12000_unseen_multiseed_baseline.yaml `
  --output-dir outputs\eval\s12000_unseen_multiseed_baseline `
  --chunk-blend 0

# B组
.\evaluate\run.ps1 `
  --checkpoint outputs\train\smolvla_ur10e_mug_v1_b8_s12000\checkpoints\012000\pretrained_model `
  --config configs\eval\mug_v1_s12000_unseen_multiseed_k4_limiter.yaml `
  --output-dir outputs\eval\s12000_unseen_multiseed_k4_limiter `
  --chunk-blend 4
```

中断后只在原命令末尾追加 `--resume`。正常 timeout 必须保留，不使用
`--rerun-failures`；只有 `control_exception` 或带 `error` 的轨迹由 `--resume` 自动重跑。

```powershell
# A组续跑
.\evaluate\run.ps1 `
  --checkpoint outputs\train\smolvla_ur10e_mug_v1_b8_s12000\checkpoints\012000\pretrained_model `
  --config configs\eval\mug_v1_s12000_unseen_multiseed_baseline.yaml `
  --output-dir outputs\eval\s12000_unseen_multiseed_baseline `
  --chunk-blend 0 --resume

# B组续跑
.\evaluate\run.ps1 `
  --checkpoint outputs\train\smolvla_ur10e_mug_v1_b8_s12000\checkpoints\012000\pretrained_model `
  --config configs\eval\mug_v1_s12000_unseen_multiseed_k4_limiter.yaml `
  --output-dir outputs\eval\s12000_unseen_multiseed_k4_limiter `
  --chunk-blend 4 --resume
```

运行期间可在另一个 PowerShell 窗口只读检查进度。正式汇总文件在整组结束时生成，
中途以 `rollouts.jsonl` 的合法行数为准：

```powershell
# A组/B组分别检查；完成时应为120。
(Get-Content outputs\eval\s12000_unseen_multiseed_baseline\rollouts.jsonl |
  Where-Object { $_.Trim() }).Count

# 整组完成后检查结果规模、唯一键和异常数。
$rows = Import-Csv outputs\eval\s12000_unseen_multiseed_baseline\rollouts.csv
$rows.Count
($rows.rollout_key | Sort-Object -Unique).Count
($rows | Where-Object { $_.failure_mode -eq 'control_exception' -or $_.error }).Count
```

若主机重启或终端中断，先确认没有同一组评测进程仍在运行，再使用原始命令加
`--resume`。两组可以分时运行，但不要在同一块 GPU 上并行运行。

## 6. 配对分析与验收

```powershell
python -m scripts.compare_control_stack `
  --baseline outputs\eval\s12000_unseen_multiseed_baseline `
  --controlled outputs\eval\s12000_unseen_multiseed_k4_limiter `
  --output outputs\eval\s12000_control_stack_comparison
```

主指标为 B−A 成功率差值及 scene 聚类配对 Bootstrap 95% CI（10,000 次，
`rng=20260813`）。同时报告每组成功率、分 seed/任务统计、改善/退化配对数、边界
jump、方向翻转率、运动指标和 limiter 触发率。

分析入口会强制检查：每组 120 条且键集合一致、checkpoint 和评测身份一致、A 为
K0/limiter 关闭、B 为 K4/limiter 开启、两组无控制异常。差值区间跨越 0 时只作
描述性报告，不宣称联合控制带来确定提升。

主机分析完成后应得到：

```text
outputs/eval/s12000_control_stack_comparison/
├── paired_rollouts.csv
├── comparison_summary.json
└── comparison_report.md
```

只回传两组正式目录和比较目录。不要把 smoke、pilot、checkpoint 或全部视频提交到
Git；需要人工审计时单独保留失败视频和指定配对视频。
