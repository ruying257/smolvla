# 环境级域随机化（纹理+光照）训练实验记录与交接

> 本文档是**交接文档**：以本次"环境级域随机化（Domain Randomization）"实验为核心，完整记录
> 背景、设计、数据、结果、分析与操作手册。任何接手人读完本文即可了解"做了什么、结果如何、
> 产物在哪、如何复现、下一步做什么"。实验时间为 2026-08 下旬，承接 `Mug_v3数据采集与验收.md`
> 的 mug 数据集与 s8000/s12000 训练链。

---

## 0. 交接摘要（TL;DR）

**一句话结论**：环境级 DR（纹理×光照、重放重渲染）训练成功——**干净性能不回归（0.80，与 s12000 持平），
纹理鲁棒性从 30-40% 提升到 70-90%，两个未见光照预设泛化 0.8-1.0，无灾难性遗忘**。验收 6 项全部通过
（`changed@alt` 0.5 压线，见 §5）。

**关键产物位置**：

| 产物 | 位置 |
|---|---|
| DR 数据集（76 条） | `smolvla-data/smolvla_ur10e_mug_dr/`（manifest: `meta/domain_manifest.json`） |
| 训练 checkpoint（s12000→6000 步 = 有效 18000） | `outputs/train/smolvla_ur10e_mug_v1_dr_b8_s18000/` |
| DR 评测输出（120 条件） | `outputs/eval/mug_robustness_dr/`（`summary.json`/`report.md`） |
| 旧基线对照 | `outputs/eval/mug_robustness_h25_s8000`、`mug_robustness_h25_s12000` |
| 像素 DR 失败对照 | `outputs/eval/mug_robustness_h25_s12000plus3000_dr` |
| 代码/配置 | 见 §6 索引 |

---

## 1. 实验目标与最终结论

### 1.1 目标

提升 mug 策略对**视觉外观变化**（杯子纹理替换、光照变化）的鲁棒性，为 sim2real 迁移打基础。
评测对象是 `smolvla_ur10e_mug_v1` 数据集训练的 SmolVLA 策略（s8000/s12000）。

### 1.2 最终结论（vs 验收标准）

| # | 验收项 | 门槛 | 实测 | 判定 |
|---|---|---|---|---|
| 1 | 重放一致率（DR 数据质量） | ≥ 98% | 平均偏差 0.00025 rad | ✅ |
| 2 | 干净 (original, default) | ≥ 0.7 | **0.80** | ✅ 无遗忘 |
| 3 | 纹理 (green_white/changed, default) | ≥ 0.6 | **0.90 / 0.70** | ✅ |
| 4 | 组合 (green_white/changed, alt) | ≥ 0.5 | **0.80 / 0.50** | ✅（changed 压线）|
| 5 | 光照 (original, alt) | ≥ 0.6 | **0.70** | ✅ |
| 6 | holdout (original, holdout_1 / holdout_2) | ≥ 0.6 / 0.5 | **0.80 / 1.00** | ✅ |

> 注：`changed@alt` 0.5、`changed@holdout_1` 0.6 被汇总器按更严的崩溃规则
> `max(0.5, baseline−20pp)=0.6` 标记为"崩溃"，但按验收门槛（≥0.5）均通过——见 §5.4。

---

## 2. 背景与动机（必要上下文）

### 2.1 评测发现的短板

对 mug 策略执行固定扰动矩阵（5 场景 × 2 任务 × policy_seed 20260，每条件 10 rollout），
旧评测（`mug_robustness_h25_s8000` / `h25_s12000`）关键结果：

| 条件 | s8000 | s12000 |
|---|---|---|
| 干净（original，默认光照） | 0.70 | 0.80 |
| 外观变体 green_white / changed | **0.30 / 0.30** | **0.30 / 0.40** |
| gaussian_blur σ4（最强档） | 0.10 | 0.20 |
| 210 条件平均 | 0.657 | 0.710 |

结论：模型对**纹理替换**（30-40%）最脆弱——这正是 sim2real 中真实世界物体外观变化的对应风险。
训练数据只有单一纹理 + 固定光照，模型学到的是"这幅图的特定外观"，而非"杯子/放置区的本质"。

### 2.2 像素级 DR 的失败复盘（反面教训）

第一次尝试：从 s12000 出发开启 LeRobot `image_transforms`（8 变换、每帧强制 3 个、强度偏高，
brightness [0.6,1.5]、blur σ[0.5,3.0]、affine 等），`--policy.path` LR 重启，训练 3000 步
（实际 2864，`save_freq=1000` 只落盘到 2000 步）。

结果：**全面退化**——干净 0.80→0.40，210 条件平均 0.710→0.405，21 个条件 0 提升 20 项下降。

根因（教训，成为后续设计铁律）：
1. **无干净锚点**：每帧必增强，训练期从未见干净图 → 表征被带偏；
2. **增强过强 + LR 重启**：把已收敛模型强力推离盆地；
3. **预算不足 + checkpoint 中途**：评测的是 2000 步的未完成模型。

### 2.3 业界实践（一句话对照）

环境级 DR（随机化纹理/光照/相机位姿，物理一致的渲染变化）是 sim2real 的正解
（RoboTwin 2.0 等）；像素级后处理对行为克隆常有害（Zhuang et al., CoRL 2024）；
随机化范围应覆盖部署分布且保留干净锚点（ADR 思想）。本次方案完全对齐。

---

## 3. 环境级 DR 方案（最终版设计）

### 3.1 核心机制：重放重渲染（无需重新遥操作）

记录动作是**绝对关节目标**，MuJoCo 物理确定，`reset(scene_seed)` 确定性复现杯子初始位姿。
按采集语义（记录 `state[t]` → 施加 `action[t]` → 25 物理步）重放，可**逐位复现**原始轨迹
（实测对齐偏差 0.00000 rad，终帧 success=True）。因此用同一批专家动作在随机化环境里重放，
**物理与成功行为完全不变，只有渲染随纹理/光照变化** → 零成本生成外观多样的成功示范。

### 3.2 光照：2 类固定预设（不随机连续采样）

| 预设 | 参数（a 主光强度 / b 方位角 / c 环境光） | 用途 |
|---|---|---|
| `default` | a=1.0, b=0°, c=1.0 | 训练 + 评测（干净锚点） |
| `alt` | a=1.4, b=+25°, c=1.2 | 训练 + 评测（明显差异） |

依据：随机连续采样多数档位柔和（实测部分种子与原图仅差 ~4% 像素）；旧评测显示光照类扰动影响有限。

### 3.3 组合变体（纹理 × 光照，5 个）

```
original+alt / green_white+default / green_white+alt / changed+default / changed+alt
```

### 3.4 数据 / 训练 / 评测设计

| 项 | 设计 |
|---|---|
| 数据集 | 40 原版锚点 + 40 DR（每条源 episode ×1 组合变体，5 组合循环）= **80 条**；单数据集内按帧均匀采样 ≈ 50% 干净 / 50% DR |
| 训练 | 从 **s12000** 出发，`--policy.path`（LR 重启），**6000 步**（80 条 ≈19,700 帧 / batch 8 ≈ 2460 步每 epoch ≈ 2.4 epoch，~5.6h），`save_freq=1000`，不开启像素增强 |
| 评测 | 3 纹理 × 4 光照预设（default/alt + **holdout_1/holdout_2 仅评测**）= 12 组合 × 5 scene × 2 task = **120 条件/模型**；基线 (original, default) |
| 遗忘防护 | 50% 锚点混合 + 6000 步 + `scheduler_peak_lr` 回退杠杆（`cloud/train.py` 已支持）+ 干净门先测 |

---

## 4. 实验数据（本次实测）

### 4.1 DR 数据集：76 条（计划 80，4 条被校验门拒绝）

| 组合 | 条数 |
|---|---|
| original@alt | 8 |
| green_white@default | 6 |
| green_white@alt | 8 |
| changed@default | 8 |
| changed@alt | 8 |
| **原版锚点（original@default）** | 38 |

- 总帧数 18,684，20 fps；重放状态偏差平均 0.00025 rad、最大 0.0013 rad；
- 76 条全部在重放中达成成功（`first_success_frame` 均有记录）；
- **拒绝 4 条**：源 episode 16/26 及其变体（green_white@default），原因 `final_success=False`。
  诊断：这两个源 episode 在当前 sim 下重放**全程 0 次成功**（机器人轨迹复现偏差 0.0002、
  杯子初始位姿完全一致），即**采集后 sim 资源可能有过变动，这两个的抓取恰在临界点**。
  校验门正确丢弃——保留会产生"动作相同但结果失败"的不一致样本。

### 4.2 训练

- 起点 s12000 → 6000 步 → 输出 `outputs/train/smolvla_ur10e_mug_v1_dr_b8_s18000`（有效 12000+6000=18000 步）；
- 调度器自动缩放：warmup 200 → peak 1e-4 → 2.5e-6@6000；`save_freq=1000`；
- ⚠️ **待确认**：服务器上 `checkpoints/last/training_state/training_step.json` 应为 **6000**（评测前必须核实，勿用中途 checkpoint）。

### 4.3 评测：DR 模型 12 组合完整表（`outputs/eval/mug_robustness_dr`）

| 组合 | 成功率 | CI [低, 高] | holdout | 崩溃标记 |
|---|---|---|---|---|
| original@default | **0.80** | [0.6, 1.0] | — | 基线 |
| original@alt | 0.70 | [0.3, 1.0] | — | |
| original@holdout_1 | 0.80 | [0.6, 1.0] | ✅ | |
| original@holdout_2 | **1.00** | [1.0, 1.0] | ✅ | |
| green_white@default | **0.90** | [0.7, 1.0] | — | |
| green_white@alt | 0.80 | [0.6, 1.0] | — | |
| green_white@holdout_1 | 0.80 | [0.6, 1.0] | ✅ | |
| green_white@holdout_2 | **1.00** | [1.0, 1.0] | ✅ | |
| changed@default | 0.70 | [0.3, 1.0] | — | |
| changed@alt | **0.50** | [0.1, 0.9] | — | ⚠️ 压线 |
| changed@holdout_1 | 0.60 | [0.3, 0.9] | ✅ | ⚠️ |
| changed@holdout_2 | **1.00** | [1.0, 1.0] | ✅ | |

执行参数：h=50、每条件 10 rollout、固定 scene/task/policy_seed、Bootstrap CI。
（对照组：s12000 跑同配置的 `mug_robustness_s12000_dr` **尚未执行**，见 §9。）

---

## 5. 结果分析

### 5.1 无灾难性遗忘 ✅

干净 `(original, default) = 0.80`，与 s12000 持平（0.8），远高于 0.7 门槛。
6000 步 + 50% 锚点混合 + LR 重启受控（`--policy.path`）的遗忘防护设计生效。
（若干净门曾失败，回退杠杆为 `train.scheduler_peak_lr: 3e-5`，见 §7。）

### 5.2 纹理鲁棒性大幅提升 ✅

与 s12000 旧矩阵同口径对比（同 10 rollout、同 seed，默认光照）：

| 条件 | s12000 | DR(s18000) | 提升 |
|---|---|---|---|
| green_white | 0.30 | **0.90** | +60pp |
| changed | 0.40 | **0.70** | +30pp |

纹理从"短板"变成"强项"，干净不掉——环境级 DR 的目标达成。

### 5.3 holdout 泛化优秀 ✅

两个**未参与训练**的光照预设：插值档 holdout_1（a=1.2/b=+15°/c=1.1）0.8、域外档 holdout_2
（a=0.7/b=-25°/c=0.9，更暗）**1.0**。模型学到的是光照不变性，而非对训练预设的记忆。

### 5.4 changed 纹理仍为相对弱项 ⚠️

- `changed@default` 0.7 与 `changed@holdout_2` 1.0 均正常 → 问题不在 changed 本身；
- `changed@alt` 0.5（压线达标）、`changed@holdout_1` 0.6 被崩溃规则标记；
- **推断**：`changed × 强光照` 组合在训练数据中仅 8 条变体 + 固定一档 alt 光照，覆盖偏薄
  （非定论，待更多数据验证）。

### 5.5 统计口径提醒

每条件仅 10 rollout，CI 宽（如 changed@alt [0.1, 0.9]）——单点 0.5 vs 0.6 的差异在噪声内，
**不应因两个标记项否定整体结论**；整体模式（干净守住 + 纹理大幅提升 + holdout 泛化）可信。

---

## 6. 代码与产物索引

### 6.1 代码/配置

| 模块 | 路径 | 说明 |
|---|---|---|
| sim 光照能力 | `sim/mug_environment.py` | `set_lighting(a,b,c)` 显式预设、`set_domain(seed)` 随机采样、`LIGHTING_*_RANGE`、`domain_summary()` |
| 数据生成工具 | `scripts/domain_randomize_dataset.py` | 重放重渲染，组合变体循环，校验门，manifest |
| 数据配置 | `configs/domain_randomize.yaml` | 2 光照预设 + 5 组合变体 + `variants_per_episode: 1` |
| 训练配置 | `configs/train/mug_dr_s12000.yaml` | steps 6000、scheduler_peak_lr 回退杠杆（注释） |
| 训练入口 | `cloud/train.py` | 非 resume 路径支持 `train.scheduler_peak_lr` → `--scheduler.peak_lr=` |
| 评测工具 | `evaluate/diagnose_mug_visual_robustness.py`、`evaluate/rollout_robustness.py` | `lighting_presets` 全交叉 + `holdout_presets` 标注 |
| 评测配置 | `configs/eval/mug_robustness/diagnose_mug_robustness_dr.yaml`（120 条件）、`diagnose_mug_robustness_dr_clean.yaml`（干净门 10 条件） | |
| 兼容修复 | `collector/common/task_spec.py` | 自包含积木任务定义（gitignore 忽略 legacy 导致 git 同步机导入失败的修复，见 §7.4） |
| 测试 | `tests/test_mug_scene.py`（DR 测试）、`tests/test_mug_visual_robustness.py`（lighting 测试）、`tests/test_cloud.py`（peak_lr） | 31 项全绿 |

### 6.2 数据与产物

| 产物 | 位置 |
|---|---|
| DR 数据集（76 条） | `smolvla-data/smolvla_ur10e_mug_dr/`（`meta/domain_manifest.json` 含组合元数据） |
| 源数据集 | `smolvla-data/smolvla_ur10e_mug_v1/`（40 条） |
| 训练 checkpoint | `outputs/train/smolvla_ur10e_mug_v1_dr_b8_s18000/` |
| 旧 checkpoint | `outputs/train/smolvla_ur10e_mug_v1_b8_s8000/`、`..._b8_s12000/`、`..._b8_s12000plus3000_dr/`（像素 DR 失败版） |
| 评测输出 | `outputs/eval/mug_robustness_dr/`（本次 DR 模型）、`mug_robustness_h25_s8000`、`mug_robustness_h25_s12000`、`mug_robustness_h25_s12000plus3000_dr`（旧矩阵基线/对照） |

---

## 7. 操作手册（复现命令 + 环境坑）

### 7.1 P2：重新生成 DR 数据集（本机 `smolvla-collector-clean`，CPU ~40 分钟）

```powershell
conda activate smolvla-collector-clean
# 数据集已存在时工具拒绝覆盖，需先删除旧版
# Remove-Item -Recurse -Force smolvla-data\smolvla_ur10e_mug_dr
python -m scripts.domain_randomize_dataset --config configs/domain_randomize.yaml
```

### 7.2 P3：训练（AutoDL 服务器，tmux 后台，~5.6h）

```bash
cd /root/autodl-tmp/smolvla && source .venv-cloud/bin/activate
export HF_HOME=/root/autodl-tmp/hf-cache
export HUGGINGFACE_HUB_CACHE=/root/autodl-tmp/hf-cache/hub
export HF_ENDPOINT=https://hf-mirror.com   # 每次新会话都要设，建议写进 ~/.bashrc
bash scripts/train.sh --config configs/train/mug_dr_s12000.yaml \
  --dataset-root smolvla-data/smolvla_ur10e_mug_dr
```

训练完成**先确认** `checkpoints/last/training_state/training_step.json = 6000`，再下载
完整 `outputs/train/smolvla_ur10e_mug_v1_dr_b8_s18000/`（不得只传 model.safetensors）。

### 7.3 P4：评测（本机 `smolvla-eval`，先干净门再全矩阵）

```powershell
conda activate smolvla-eval
# ① 干净门（10 条件，~15 分钟）：(original, default) ≥ 0.7
python -m evaluate.diagnose_mug_visual_robustness --checkpoint outputs\train\smolvla_ur10e_mug_v1_dr_b8_s18000 `
  --config configs\eval\mug_robustness\diagnose_mug_robustness_dr_clean.yaml `
  --output-dir outputs\eval\mug_robustness_dr_clean
# ② 全矩阵（120 条件，~2-2.5h）
python -m evaluate.diagnose_mug_visual_robustness --checkpoint outputs\train\smolvla_ur10e_mug_v1_dr_b8_s18000 `
  --config configs\eval\mug_robustness\diagnose_mug_robustness_dr.yaml `
  --output-dir outputs\eval\mug_robustness_dr
# ③ s12000 对照组（同配置）
python -m evaluate.diagnose_mug_visual_robustness --checkpoint outputs\train\smolvla_ur10e_mug_v1_b8_s12000 `
  --config configs\eval\mug_robustness\diagnose_mug_robustness_dr.yaml `
  --output-dir outputs\eval\mug_robustness_s12000_dr
```

中途边训边评：训练在服务器继续，下载已落盘的 `checkpoints/<N>` 数字目录到本机后即可评测
（`checkpoints/last` 是符号链接，scp 默认不跟随）；中途 checkpoint 表现通常低于最终步，只看趋势。

### 7.4 服务器/环境坑记录（本次踩过的）

1. **HF 连接失败**：`OSError: We couldn't connect to 'https://huggingface.co'` →
   设置 `export HF_ENDPOINT=https://hf-mirror.com`（国内直连 huggingface.co 不通）；
2. **huggingface-hub 版本冲突**：环境里 `huggingface-hub==1.28.0` 与 `transformers==4.57.1`
   （要求 `<1.0`）冲突 → 必须对齐项目锁定 **`huggingface-hub==0.35.3`**（`constraints.txt`），
   并 `rm -rf` 后用 0.35.3 重下缓存；任何依赖漂移以 `constraints.txt` 为准、`pip check` 体检；
3. **git 同步缺文件**：`.gitignore` 的 `*/legacy/` 规则使 `collector/common/legacy/` 不进 git，
   实验室主机（git 同步）缺该目录 → 修复为 `collector/common/task_spec.py` 自包含内嵌定义；
4. **wandb 卡登录**：`wandb.enable=true` 且未登录时可能卡交互 → `wandb login` 或
   `export WANDB_MODE=offline`。

---

## 8. 决策记录与可调项（最终定稿）

| 决策点 | 最终决定 | 理由 | 可调方向 |
|---|---|---|---|
| 范围 | 只纹理 + 光照 | 用户确认；像素/相机位姿排除 | Phase 2 可加相机位姿 |
| 纹理 | 3 个现有变体 | 与评测一致 | 可程序化生成更多 |
| 光照 | 2 类固定预设（default/alt） | 随机采样档位柔和、光照影响有限 | alt 强度可调 |
| 变体形式 | 纹理×光照合并（5 组合） | 光照不值得单独一类 | 组合可增删 |
| 数据方法 | 重放重渲染 | 无需遥操作、物理一致（偏差 0.0） | 新场景需人工采集 |
| 数据规模 | 40 原版 + 40 DR（实际 76 条） | 50/50 锚点混合 | 锚点比例可调（60/40） |
| 训练起点 | s12000 | 保留已学技能 | s8000（对照更干净） |
| 训练步数 | 6000（2.4 epoch，~5.6h） | 小于原版量级 + 抑制遗忘震荡 | 4000-8000 可调 |
| 遗忘防护 | 锚点 + 6000 步 + peak_lr 杠杆 + 干净门 | LR 重启→遗忘震荡为已记录风险 | 检出遗忘→先降 peak_lr |
| 评测 | 3×4 预设（含 2 holdout）= 120 条件 | 验证域内 + 未见光照泛化 | holdout 数量/强度可调 |

---

## 9. 遗留事项与下一步

1. **s12000 新矩阵对照组未跑**：`outputs/eval/mug_robustness_s12000_dr` 尚未生成（命令见 §7.3 ③）。
   补上后可在完全同口径（含光照预设）下给出 s12000 vs DR 的提升数值；
2. **训练完整性最终确认**：服务器上 `training_step.json` 应 = 6000（§4.2）；
3. **changed 纹理弱项**（可选 Phase 2）：DR 数据中增加 changed 变体条数或为其加入更强调的
   强光照档，再复测 `changed@alt` / `changed@holdout_1`；
4. **git 提交待整理**：本次新增/修改的代码、配置、测试、本文档尚未整理成提交（文件清单见 §6.1）；
5. **可扩展方向**：相机位姿随机化（Phase 2）、更多纹理、桌面/背景纹理随机化。

---

## 10. FAQ

- **为什么这次没重蹈像素 DR 失败？** ① 增强物理一致（非无差别图像处理）；② 50% 原版锚点
  混合采样；③ 评测预设与训练预设同源、holdout 才外推；④ 重放偏差 0.0 保证数据质量。
- **会不会灾难性遗忘？** 实测干净 0.80 未回归（三层防护 + 干净门见 §5.1）；若未来重训检出
  遗忘，回退杠杆为 `scheduler_peak_lr: 3e-5` 或增原版比例。
- **光照/纹理变化影响物理吗？** 不影响——MuJoCo 光照只参与渲染、纹理只改视觉网格。
- **为什么不用人工重新采集？** 重放重渲染用同一批专家动作生成外观多样数据（CPU 小时级）；
  人工采集只用于"新场景布局/新动作模式"。
- **holdout 预设的意义？** holdout_1/holdout_2 不参与训练只在评测，验证未见光照泛化，
  防止只对训练预设过拟合（实测 0.8-1.0，说明无过拟合）。
- **训练中怎么看是否正常？** loss 前几百步略升后回落 ~0.01-0.02；LR warmup 200→1e-4→2.5e-6
  平滑衰减；`save_freq=1000` 每 ~56 分钟落盘；结束确认 `training_step.json = 6000` 再评测。
