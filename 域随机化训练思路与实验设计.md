# 环境级域随机化（Domain Randomization）训练思路与实验设计

> 本文档梳理"为什么做域随机化 → 第一次尝试为何失败 → 业界怎么做 → 当前环境级方案全流程"，
> 作为后续实验与决策的记录。数据来源：`outputs/eval/` 下的鲁棒性评测产物、`outputs/train/` 下的
> 训练日志与 checkpoint、LeRobot 0.4.4 / MuJoCo 源码与实测。

---

## 1. 问题背景：为什么需要域随机化

### 1.1 评测发现的鲁棒性短板

对 mug 策略执行固定扰动矩阵（5 个已见场景 × 2 任务 × 固定 policy_seed=20260，每条件 10 次 rollout，
`configs/eval/mug_robustness/diagnose_mug_robustness.yaml`），三个模型的成功率：

| 条件 | s8000 (008000) | s12000 | s12000+像素DR(失败) |
|---|---|---|---|
| **干净 original（无扰动）** | **0.70** | **0.80** | 0.40 |
| **210 条件平均** | **0.657** | **0.710** | 0.405 |
| 外观变体 green_white / changed | 0.30 / 0.30 | 0.30 / 0.40 | 0.20 / 0.20 |
| gaussian_blur σ1/2/4 | 0.7 / 0.5 / **0.1** | 0.9 / 0.7 / 0.2 | 0.7 / 0.2 / 0.2 |
| gaussian_noise σ5/15/30 | 0.9 / 0.5 / 0.5 | 0.9 / 0.7 / 0.5 | 0.5 / 0.5 / 0.2 |
| brightness ×0.5/0.7/0.9 | 0.8 / 0.8 / 0.6 | 0.9 / 0.7 / 0.8 | 0.5 / 0.4 / 0.4 |
| contrast / gamma / jpeg | 0.6-1.0 | 0.6-0.9 | 全部 ≤0.6 |

**结论**：模型对**外观纹理替换**（30-40%）和**强模糊**（σ4 仅 10-20%）最脆弱；这些正是
sim2real 中真实世界相机/物体外观变化对应的风险。目标是让策略对这些视觉变化鲁棒。

### 1.2 为什么普通训练学不到鲁棒性

训练数据 `smolvla_ur10e_mug_v1`（40 条）只包含**单一纹理 + 固定光照 + 固定相机位姿**的渲染。
模型学到的是"这幅图的特定外观"与动作的关联，而不是"杯子/放置区的本质"与动作的关联——
一旦外观分布偏移（换纹理、换光照），策略退化。

**域随机化（DR）** 就是在训练时让观测分布覆盖部署时可能遇到的变化范围，强迫模型学习
**外观不变特征**。

---

## 2. 概念澄清：像素级 DR vs 环境级 DR

| 维度 | 像素级 DR（图像后处理） | 环境级 DR（渲染参数） |
|---|---|---|
| 改变方式 | 训练时对相机图像做亮度/对比度/噪声/模糊/仿射等后处理 | 改变 MuJoCo 渲染参数（纹理/光照/相机位姿），**物理与几何不变** |
| 与真实世界的一致性 | 近似，但破坏任务相关像素（如物体边缘、末端执行器） | **物理一致**：改的是"世界长什么样"，不是"图像被怎么处理" |
| 典型做法 | LeRobot `dataset.image_transforms`（ColorJitter 等） | 采集/重渲染时随机化纹理、光照、相机位姿 |
| 业界观点 | 对行为克隆（BC）**常常效果不达标甚至有害**（Zhuang et al., CoRL 2024：常规 Random Crop/Color Jitter 会破坏任务关键区域） | sim2real 的主流正确路线（RoboTwin 2.0 等：随机化纹理/光照/相机位姿） |

**关键区分**：像素级 DR 改的是"图像的成像过程"，环境级 DR 改的是"世界本身"。后者生成的变化
与真实部署（不同杯子外观、不同灯光、不同拍摄角度）物理一致，模型学到的不变性才能迁移到真实世界。

---

## 3. 第一次尝试的教训：像素级 DR 为什么失败

### 3.1 做了什么

从 s12000 出发（`--policy.path`，LR 重启到 1e-4），开启 LeRobot `image_transforms`
（**8 个变换、每帧强制 3 个**：brightness [0.6,1.5]、contrast [0.6,1.4]、GaussianNoise σ0.06、
GaussianBlur σ[0.5,3.0]、affine ±5°/5%、saturation/hue/sharpness），训练 3000 步（实际跑到 2864，
`save_freq=1000` 只落盘到 2000 步 checkpoint）。

### 3.2 结果：全面退化

- 干净 0.80 → **0.40**；210 条件平均 0.710 → **0.405**；21 个条件 **0 提升、20 项下降**；
- 崩溃阈值全面恶化（brightness 0.7、gamma 0.6、blur 2.0 就崩）。

### 3.3 根因分析（对照训练日志与配置）

1. **增强分布离干净分布太远**：每帧必增强 3 个变换且强度偏高（brightness 下限 0.6 很暗、
   blur σ 到 3.0、affine 几何扰动），训练期**从未见过干净图** → 模型表征被带偏，回到干净图
   反而不认识（干净 0.4）；
2. **LR 重启 + 全新优化器**：把已收敛模型（LR ~1e-5）强力推开到 1e-4，短预算内既没学会
   扰动不变性、又丢了原特征；
3. **预算不足**：3000 步 ≈ 2.4 epoch，且评测的是**中途 2000 步**的 checkpoint（后 864 步未落盘）。

### 3.4 教训（成为后续设计的铁律）

- **保留干净锚点**：训练数据必须混入原版（无增强/默认外观）样本，让干净分布始终存在；
- **增强强度保守**：覆盖目标分布即可，不越界；微调时尤其温和；
- **机制温和**：从已收敛模型出发时，避免过强的 LR 重启；
- **评测前确认 checkpoint 是最终步**（`checkpoints/last` 的 `training_step.json` = 计划总步数）。

---

## 4. 业界最佳实践（调研对照）

| 最佳实践 | 来源 | 我们如何对齐 |
|---|---|---|
| 随机化范围 = 部署/测试分布，**不越界**；先窄后扩（课程式） | Tobin et al. sim2real 讲义；OpenAI ADR（2002.07911） | 光照档位落在训练采样区间内；范围常量可调，先保守 |
| BC 中常规增强常破坏任务关键区域，需**显著区域感知** | Zhuang et al., CoRL 2024（2608.11870） | 放弃无差别像素增强，改用物理一致的环境级 DR |
| 微调预训练模型：增强**概率<1**、强度远小于从头训练、监控干净指标 | 视觉-语言模型微调共识 | DR 数据集混入 40 条原版做锚点；验收含干净不回归 |
| 环境级 DR（纹理/光照/相机）是 sim2real 正解 | RoboTwin 2.0（2506.18088）等 | 本次核心思路 |
| LeRobot 默认：image_transforms **默认关闭**、默认强度温和 | lerobot PR #234、官方文档 | 本次不开启像素增强 |

---

## 5. 当前方案：环境级 DR（纹理 + 光照）

### 5.1 核心机制：重放重渲染（无需重新遥操作）

**原理**：记录动作是**绝对关节目标**（6 关节角 + 夹爪 0/1），MuJoCo 物理确定，
`reset(scene_seed)` 确定性复现杯子初始位姿。因此在随机化环境里按**采集语义**重放
（记录 `state[t]` → 施加 `action[t]` → 推进 25 个物理步）可**逐位复现**原始轨迹
（已实测：对齐后偏差 **0.00000 rad**，终帧 `evaluate_task: success=True`）。
物理完全一致，只有渲染随纹理/光照变化 → **同一批专家动作可生成物理一致、外观多样的
新 episode，成功行为保持不变**。

**验证过的关键事实**：
- 采集语义：20 Hz 设 ctrl，帧间 25 个物理步（timestep=2ms，60Hz viewer 累积）；
- 光照：`set_lighting(a, b, c)` 显式写入 `model.light_*`（运行时，无需重载），物理不变；
  `set_domain(seed)` 保留为随机采样探索接口，数据生成改用固定预设；
- 纹理：模型编译时注入纹理字节（`_load_mug_model`，3 变体），换纹理需重载模型；
- 变体渲染确实不同（冒烟实测与原版像素差 77%）。

### 5.2 数据设计：`smolvla_ur10e_mug_dr`（80 条，组合变体版）

**修订依据**（用户反馈 + 实测）：旧版随机采样的三维光照多数档位柔和（部分种子与原图仅差 ~4%
像素），且旧评测显示光照类扰动（brightness/gamma/contrast）影响有限 → 光照改为 **2 类固定预设**，
**纹理+光照合并为组合变体**，不再单独一类。

```
光照预设（2 类，固定参数）：
  default: a=1.0, b=0°,  c=1.0   （场景默认 = 干净锚点）
  alt:     a=1.4, b=+25°, c=1.2  （固定"明显差异"预设；强度可调，P2 目检确认）

40 条原版（original + default）          ← 干净锚点
+ 40 条 DR（每条源 episode ×1 个组合变体，5 组合循环，每组合 8 条）：
  original+alt / green_white+default / green_white+alt / changed+default / changed+alt
合计 80 条 ≈ 19,700 帧；单数据集内按帧均匀采样 ≈ 50% 干净 / 50% DR
```

- 工具：`scripts/domain_randomize_dataset.py`（配置 `configs/domain_randomize.yaml`，显式
  `variants` 组合列表 + `lighting_presets`）；
- 校验门：重放终态与记录偏差 ≤0.02 rad 且任务成功保留，失败变体丢弃；
- 每条 episode 的组合元数据（纹理、光照预设名与参数、源 episode）写入
  `meta/domain_manifest.json`，保证训练与评测可复现。

### 5.3 训练设计：从 s12000 出发，6000 步

- **起点**：s12000（`--policy.path` 加载权重，LR 重启；数据集分布已变，重启合理）；
- **步数**：6000 ≈ **2.4 epoch**（80 条 ≈ 19,700 帧 / batch 8 ≈ 2460 步每 epoch）。
  依据：环境级 DR 是"视觉分布适配"而非"重新学任务"（s12000 已收敛）；6000 步在"明显小于
  原版训练量级（8000-10000）"与"给锚点+DR 充分曝光以抑制遗忘震荡"之间取平衡；
  成本 ~5.6h（3.38s/step），`save_freq=1000` 每 ~56 分钟落盘可续；
- **不开启像素增强**（环境级 DR 已覆盖，避免 3.3 的分布偏移问题）；
- **遗忘震荡防护**：`--policy.path` LR 重启到 1e-4 是已记录风险；6000 步 + 50% 干净锚点缓解，
  并预留回退杠杆 `train.scheduler_peak_lr`（如 3e-5 → `--scheduler.peak_lr=`，`cloud/train.py`
  已支持）；
- 配置：`configs/train/mug_dr_s12000.yaml`（输出 `smolvla_ur10e_mug_v1_dr_b8_s18000`，
  12000+6000=有效 18000 步）。

### 5.4 评测设计：外观×光照全交叉（含 holdout）

- 新配置 `configs/eval/mug_robustness/diagnose_mug_robustness_dr.yaml`：
  - **3 纹理 × 4 光照预设全交叉** = 12 组合 × 5 scene × 2 task = **120 条件/模型**（~2-2.5h）；
  - 预设：`default` / `alt`（训练同源参数）+ **`holdout_1`（a=1.2,b=+15°,c=1.1）/ `holdout_2`
    （a=0.7,b=-25°,c=0.9）仅评测**不参与训练**，验证未见光照泛化**；
  - **不含像素扰动、不含相机位姿**（已确认范围）；
- 工具：`evaluate/diagnose_mug_visual_robustness.py` 的 lighting 机制为 `lighting_presets`
  （预设 + 全交叉）+ `holdout_presets` 标注；`run_single_rollout` 注入显式光照参数。

---

## 6. 完整流水线（P1-P4）

| 阶段 | 内容 | 环境 | 命令 / 产物 |
|---|---|---|---|
| P1 能力 | sim 光照能力 + 测试 | 本机（无 GPU） | `sim/mug_environment.py`（set_lighting/set_domain）；`python -m unittest tests.test_mug_scene` |
| P2 数据 | 重放重渲染生成 DR 数据集（**先删除旧版 114 条数据集**） | 本机 smolvla-collector-clean（CPU ~40 分钟） | `python -m scripts.domain_randomize_dataset --config configs/domain_randomize.yaml` → `smolvla_ur10e_mug_dr`（**80 条**） |
| P3 训练 | 从 s12000 训练 6000 步 | 腾讯云（~5.6h，tmux） | `bash scripts/train.sh --config configs/train/mug_dr_s12000.yaml --dataset-root smolvla-data/smolvla_ur10e_mug_dr` → 回传 `outputs/train/smolvla_ur10e_mug_v1_dr_b8_s18000` |
| P4 评测 | appearance × lighting 全交叉（含 holdout） | 本机 smolvla-eval | **先测干净门**（(original,default) 10 rollout）→ `python -m evaluate.diagnose_mug_visual_robustness --checkpoint <dr> --config configs/eval/mug_robustness/diagnose_mug_robustness_dr.yaml --output-dir outputs/eval/mug_robustness_dr`；**同步对 s12000 跑同配置**作对比 |

## 7. 验收标准

1. **重放一致率 ≥ 98%**（终态偏差容差内），DR 数据集 **80 条**契约完整（含组合元数据）；
2. **干净不回归（遗忘门，P4 第一步）**：(original, default) **≥ max(0.6, s12000干净−0.1) = 0.7**；
3. **纹理鲁棒**：(green_white, default) / (changed, default) → **≥ 60%**；
4. **组合鲁棒**：(green_white, alt) / (changed, alt) → **≥ 50%**；
5. **光照影响验证**：(original, alt) **≥ 60%**；
6. **holdout 泛化**：(original, holdout_1) **≥ 60%**（插值档），(original, holdout_2) **≥ 50%**（域外档）；
7. 像素扰动不作为验收项（已排除）。

## 8. 决策记录与可调项

| 决策点 | 当前决定 | 理由 | 可调方向 |
|---|---|---|---|
| 范围 | 只纹理 + 光照 | 用户确认；像素/相机位姿排除 | 后续可加相机位姿（Phase 2） |
| 纹理 | 3 个现有变体 | 与评测一致，无需新资源 | 可程序化生成更多纹理 |
| 光照 | **2 类固定预设**（default/alt），不再随机连续采样 | 随机采样多数档位柔和、光照影响有限 | alt 强度可调（P2 目检） |
| 变体形式 | **纹理×光照合并组合变体**（5 组合） | 光照不值得单独一类（用户确认） | 组合可增删 |
| 数据方法 | 重放重渲染 | 无需遥操作，物理一致（实测偏差 0.0） | 如需新场景/新动作，人工采集 |
| 数据规模 | **40 原版 + 40 DR = 80 条**（50/50 锚点混合） | 干净锚点 + 步数减半 | 锚点比例可调（60/40） |
| 训练起点 | s12000 | 保留已学技能 | s8000（对照更干净） |
| 训练步数 | **6000（2.4 epoch，~5.6h）** | 小于原版量级 + 抑制遗忘震荡 | 4000-8000 可调 |
| 遗忘防护 | 锚点混合 + 6000 步 + `scheduler_peak_lr` 回退杠杆 + 干净门 | LR 重启→遗忘震荡是已记录风险 | 检出遗忘→先降 peak_lr |
| 评测 | appearance×lighting 全交叉（**含 2 个 holdout 预设**），无像素 | 与 DR 范围一致 + 验证未见光照泛化 | holdout 数量/强度可调 |

## 9. 常见问题 FAQ

- **为什么这次不会重蹈像素 DR 失败？** ① 增强物理一致（不是无差别图像处理）；② 混入 40 条
  原版干净锚点（50/50 混合采样）；③ 强度保守且评测档位与训练预设同源；④ 重放偏差 0.0 保证数据质量。
- **会不会灾难性遗忘/遗忘震荡？** 已做三层防护：50% 干净锚点持续监督（动作逐帧不变，任务级
  遗忘风险低）、6000 步充分曝光、`scheduler_peak_lr` 回退杠杆；并以干净门（≥ max(0.6, s12000−0.1)）
  在评测第一步直接检测，检出即回退。
- **为什么不用人工重新采集？** 重放重渲染用同一批专家动作生成外观多样数据，成本是 CPU 小时级；
  人工采集只用于"新场景布局/新动作模式"（当前任务无此需求）。
- **光照/纹理变化会影响物理吗？** 不会——MuJoCo 光照只参与渲染，纹理只改变视觉网格，
  碰撞/动力学完全不变，这是环境级 DR 的核心优势。
- **holdout 预设的意义？** holdout_1/holdout_2 不参与训练，只在评测使用，验证模型对
  **未见光照**的泛化（插值档 holdout_1、近似域外档 holdout_2），防止只对训练预设过拟合。
- **训练中怎么看是否正常？** loss 前几百步会略升（新分布适应），随后回落到 ~0.01-0.02 区间；
  LR 按 warmup 200 → 1e-4 → 2.5e-6 平滑衰减；`save_freq=1000` 每 ~56 分钟落盘，
  结束确认 `checkpoints/last/training_state/training_step.json = 6000` 再评测。

## 10. 附：本方案的代码与文档索引

- sim 能力：`sim/mug_environment.py`（`set_lighting` / `set_domain` / `LIGHTING_*_RANGE`）
- 数据工具：`scripts/domain_randomize_dataset.py`、`configs/domain_randomize.yaml`
- 训练：`configs/train/mug_dr_s12000.yaml`（`scheduler_peak_lr` 回退杠杆经 `cloud/train.py`）
- 评测：`evaluate/diagnose_mug_visual_robustness.py`（lighting_presets 全交叉 + holdout）、
  `configs/eval/mug_robustness/diagnose_mug_robustness_dr.yaml`
- 兼容修复：`collector/common/task_spec.py`（清理遗留的导入回归）
- 测试：`tests/test_mug_scene.py`（DR 测试）、`tests/test_mug_visual_robustness.py`（lighting 测试）、
  `tests/test_cloud.py`（peak_lr 覆盖）
- 既有评测基线：`outputs/eval/mug_robustness_h25_s8000`（= 旧 `mug_robustness_baseline`）、
  `mug_robustness_h25_s12000`、失败对照 `mug_robustness_h25_s12000plus3000_dr`
