policy:
  model_id: lerobot/smolvla_base
  device: cuda
  use_amp: true
  empty_cameras: 1

dataset:
  repo_id: smolvla_ur10e
  video_backend: pyav

train:
  output_dir: outputs/train/smolvla_ur10e
  job_name: smolvla_ur10e
  seed: 1000
  batch_size: 1
  steps: 20000
  num_workers: 2
  log_freq: 20
  save_freq: 1000
